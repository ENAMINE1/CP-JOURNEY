{
    "name": "C++ Development",
    "image": "mcr.microsoft.com/devcontainers/cpp:debian",
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": []
        }
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y g++-11",
    "runArgs": ["--init"]
}
